<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coloring Book | Art by {{ image.artist }}</title>

    <script src="https://kit.fontawesome.com/080c06f1d6.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" type="text/css" href="../css/main.css" />
    <script src="https://cdn.jsdelivr.net/npm/@jaames/iro@5"></script>
</head>
<body  id="top" >
<header>
  <div class="content__header">
    <button class="accessibility-button" onclick="checkAccessibility()">
      <i class="fas fa-universal-access"></i>
    </button>
    <h1>Coloring Book</h1>
    <div id="image-gallery">
      <h2 class="gallery-text">gallery view</h2>
      <img src="../images/icons/gallery.svg" alt="">
    </div>
  </div>
  <div id="image-gallery_modal">
    <div class="wrapper">
      {% for image in data.images -%}
      <a href="./{{ image.id }}.html" class="gallery-item {% if image.id in page.url %}active{% endif %}">
          <div class="thumbnail">
              <img src="../images/{{ image.src }}" alt="Artwork created by {{ image.artist }}">
          </div>
          <div class="gallery-info">
            <p class="small">Art by:</p>
            <h3 class="attribution">{{ image.artist }}</h3>
          </div>
      </a>
    {% endfor -%}
    </div>
  </div>
</header>
    
    {{ content | safe }}

  <script src="../scripts/index.js"></script>
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
</body>
</html>